name: PR Verification

on: pull_request

permissions: {}

jobs:
  lint:
    name: ShellCheck Lint
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run ShellCheck
        shell: bash
        run: |
          set -euo pipefail
          shellcheck setup.sh scripts/*.sh scripts/hooks/*

  test-install:
    name: Dry-Run Installation
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Mock Git user
        shell: bash
        run: |
          set -euo pipefail
          git config --global user.name "Test User"
          git config --global user.email "test@example.com"

      - name: Run Installer Script
        shell: bash
        run: |
          set -euo pipefail
          ./setup.sh

      - name: Verify Hooks and Safety Wrapper Installation
        shell: bash
        run: |
          set -euo pipefail
          # Assert hook binaries were copied and made executable
          [[ -x "$HOME/.githooks/pre-commit" ]]
          [[ -x "$HOME/.githooks/pre-push" ]]
          
          # Assert bashrc loader was appended
          grep -q "bcgov/agent-guardrails" "$HOME/.bashrc"
          
          # Assert safety wrapper shell script is present
          [[ -x "$HOME/.githooks/git-safety.sh" ]]

      - name: Run Git Config Setup (Non-interactive Pipe)
        shell: bash
        run: |
          set -euo pipefail
          ./scripts/git-setup.sh < /dev/null

  results:
    name: Verification Results
    runs-on: ubuntu-24.04
    needs: [lint, test-install]
    if: always()
    steps:
      - name: Check Status
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ needs.lint.result }}" == "failure" || "${{ needs.test-install.result }}" == "failure" ]]; then
            echo "CI Verification failed." >&2
            exit 1
          fi
          echo "CI Verification passed."
